#include <iostream>
#include "Sort.h"
#include "Test.h"
#include <assert.h>

int main (int argc, const char* argv[])
{
//	assert(false && "");
//	SortManualTest(SelectionSort);
}

// int argc (argument count) - аргумент отвечающий за количество параметров командной строки (всегда целочисленный)
// const char* argv (argument value) - аргумент отвечающий за значения параметров, параметры передаются в виде строчек текста

// cls очистить консоль
// ctrl + enter подставить название в консоль
// cd change directory
// ctrl + O - открыть проводник

//сделать документацию функций
//сделать документацию main
//через assert проверять параметры функций

//настроить среду



/*	for (int i = 0; i < argc; ++i)
	{
		printf ("argv [%d] = %s\n", i, argv[i]);
	}

	for (int i = 0; i < argc; ++i)
	{

		if (strcmp (argv[i], "-h")     == 0 ||
			strcmp (argv[i], "--help") == 0 ||
			strcmp (argv[i], "/?")     == 0)
		{
			printf("Description");
		}
	}

	SortTestAutoGenerate(BubleSort, 1);
*/